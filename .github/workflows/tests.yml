name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    timeout-minutes: 15
    runs-on: ubuntu-24.04
    steps:
      - name: Check out repository
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false

      - name: Provision sandbox test environment
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq bubblewrap >/dev/null
          git config --global user.name "ci"
          git config --global user.email "64233280+ghosty93@users.noreply.github.com"

      - name: Run deterministic suite
        run: python3 -m unittest discover -s tests -v

      - name: Validate JSON files
        run: |
          python3 -m json.tool schemas/coding-job.schema.json >/dev/null
          python3 -m json.tool examples/policy.json >/dev/null

      - name: Compile Python sources
        run: python3 -m compileall -q broker client plugin
